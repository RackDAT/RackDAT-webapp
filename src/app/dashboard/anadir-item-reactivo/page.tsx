import React from "react";
import AnadirForm from "./anadirForm";
type Props = {};

const page = (props: Props) => {
  return (
    <div>
        <AnadirForm />
    </div>
    );
};

export default page;